---
title: Configuration
description: The canonical catalog of span attributes, metrics and config toggles
weight: 20
---

# Configuration

Everything the addon emits and every switch that controls it.

- **[Reference](reference.md)** — every span attribute, every metric, every
  `config/statamic-telemetry.php` toggle, and the exact events each one derives
  from.
